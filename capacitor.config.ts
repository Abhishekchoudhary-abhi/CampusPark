import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.campuspark.app',
  appName: 'CampusPark',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
